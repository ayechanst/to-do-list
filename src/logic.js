export function buttonLogic() {
  console.log('hello');
  const createToDoItem = document.getElementById('createToDoItem');

  createToDoItem.addEventListener('click', () => {
    // load the form that asks for the to do item info
  });
}
